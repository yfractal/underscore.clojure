(ns underscore.core-test
  (:require [clojure.test :refer :all]
            [midje.sweet :refer :all]
            [underscore.core :refer :all]))

;(defn f [])
;(defn f1 [a])
;(defn f2 [a b])
;(deftest get-fun-full-name
;  (is (#'underscore.core/get-full-name f)  "underscore.core_test/f"))

;;in let? #() fn??
;(fact "get acture fun"
      ;(#'underscore.core/acture-fun "underscore.core_test/f") =>
      ;)

;(future-fact "know how many arguments the a function needs"
      ;(num-of-arguments f1) => 2
      ;(num-of-arguments f2) => 2)

;(deftest get-num-of-arguments
 ; (is (num-of-arguments f1) 1)
  ;(is (num-of-arguments f2) 2))
